{
  "name": "meshyapi",
  "version": "1.0.0",
  "description": "1. `.env`에 `MESHY_API_KEY=msy_...` 설정 (`.env.example` 참고) 2. 시트를 `input/<캐릭터id>/`에 배치 3. (권장) `front.png` / `left.png` / `back.png` / `right.png`로 크롭 (얼굴 컷은 `face.png`) 4. `node scripts/run_batch.mjs` (`--dry-run`으로 매칭 확인, `--only id1,id2`, `--force`)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "sharp": "^0.35.4",
    "three": "^0.185.1"
  }
}
